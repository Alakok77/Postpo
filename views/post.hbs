<!DOCTYPE html>
<html lang="en">
{{> baseHead customTitle=customTitle}}
<body>
    {{> siteHeader}}
    
    {{#if onePost}}
        <div>
            <h2>{{onePost.title}}</h2>
            <p>
                <span>{{onePost.from}} | </span>
                <span>{{onePost.created_at_text}}</span>
            </p>
            <p>{{onePost.content}}</p>
        </div>

        {{#if postComment}}
            <p>{{postComment.length}} โพดโพ</p>
            {{#each postComment}}
                <div>
                    <p>
                        <span>{{this.from}} | </span>
                        <span>{{this.created_at_text}}</span>
                    </p>
                    <p>{{this.content}}</p>
                </div>
            {{/each}}
        {{else}}
            <p>ยังไม่มีใครมาเลย คุณอยากเริ่มการโพดโพมั้ย</p>
        {{/if}}

    {{else}}
        <p>ไม่มีใครโพดโพเรื่องนี้</p>
    {{/if }}

    
    
    {{> siteFooter}}
</body>
</html>